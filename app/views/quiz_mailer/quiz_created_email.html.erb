<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #ddd;
      border-radius: 8px;
    }
    .header {
      font-size: 18px;
      font-weight: bold;
      color: #333;
    }
    .footer {
      font-size: 12px;
      color: #666;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      Hello <%= @student.name %>,
    </div>
    <h1>New Quiz Created: <%= @quiz.title %></h1>
    <p><strong>Description:</strong> <%= @quiz.description %></p>
    <p><strong>Start Time:</strong> <%= @quiz.start_time.strftime('%I:%M %p') %></p>
    <p><strong>End Time:</strong> <%= @quiz.end_time.strftime('%I:%M %p') %></p>
    <p><strong>Due Date:</strong> <%= @quiz.due_date.strftime('%d-%m-%Y') %></p>

    <h2>Questions:</h2>
    <ul>
      <% @questions.each do |question| %>
        <li>
          <strong><%= question.title %>:</strong>
          <p><%= question.description %></p>
          <p><strong>Duration:</strong> <%= question.duration || 0 %> minutes</p>
        </li>
      <% end %>
    </ul>
    <p>Best regards,</p>
    <p>The Koi Viva Voice Team</p>
    <div class="footer">
      This is an automated message. Please do not reply directly to this email.
    </div>
  </div>
</body>
</html>
